import mutations from '@/store/modules/files/mutations';
import actions from '@/store/modules/files/actions';

const state = {
  userFiles: [],
};

export default {
  state,
  mutations,
  actions,
};
